<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ThemeStyleIcons" parent="Theme.Material3.Light">
        <!-- Deixa barra de status transparente -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/azul_claro</item>
        <item name="colorPrimaryVariant">@color/azul_escuro</item>
        <item name= "colorOnPrimary">@color/preto</item>
        <item name= "android:textColorLink">@color/azul_escuro</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <color name="corSelecionado">@color/azul_transparente</color>
</resources>