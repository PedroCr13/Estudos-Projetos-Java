/*
	exemplo de interface funcional (apenas um metodo abastrato)
	nao necessidade de declarar abstract
	por estar na interface e não ter implementação
	será implicitamente abstrato
*/
interface MyValue{
	double getValue();
}